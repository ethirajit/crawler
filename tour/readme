Dependency
1. Python3
2. Pip3

Python Packages:
1. Scrapy (pip install scrapy)
2. tldextract (pip install tldextract)

Not Need - Just for future use
3. Splash (pip install splash)
4. splash-scrapy (pip install splash-scrapy)
5. flask (pip install flask)
6. sudo apt-get install python3-pyqt5
7. sudo apt-get install python3-pyqt5.qtwebki
3. scrapoxy (pip install scrapoxy)

How to run:
1. python test.py


Known Issue:
1. If 127.0.0.1 is opened in browser we get error as 'proc'  not defined.
	if proc.poll() == None:


Scrapy Depentant:
sudo apt install python3.6-dev
sudo apt-get install build-essential libssl-dev libffi-dev python-dev libxml2-dev